/**
 * @file KeybindsTab.tsx
 * @description "Keybinds" tab body for the Phase 1 PreferencesMenu shell.
 *
 * Step 8 landing: placeholder body. Keybind authoring is handled by the
 * dedicated keybind UI and is not migrated in Phase 1.
 */

import { Box, Section } from 'tgui-core/components';

export function KeybindsTab() {
  return (
    <Section title="Keybinds">
      <Box color="label">
        Keybind authoring remains on the dedicated keybind panel. This tab is a
        placeholder to match the planned final taxonomy.
      </Box>
    </Section>
  );
}
